{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=10"
  },
  "scripts": {
    "build": "npm run build:clean && lerna run build",
    "build:clean": "lerna exec --no-bail -- rimraf dist/**",
    "deploy": "lerna run deploy",
    "lerna:bootstrap": "npm install && npm run build",
    "lerna:clean": "npm run build:clean && lerna clean --yes"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
